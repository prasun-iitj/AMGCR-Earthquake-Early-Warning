project:
  name: AMGCR_Earthquake_Research
  version: 0.1.0
  python_version: "3.11+"

paths:
  data_root: data
  raw_data: data/raw
  processed_data: data/processed
  output_dir: data/output
  logs_dir: logs

logging:
  level: INFO
  file: logs/amgcr.log
